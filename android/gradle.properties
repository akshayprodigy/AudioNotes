# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
# Raised from the 2g default: the native build (whisper + sherpa + llama) OOMs below this.
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# ABIs to build. Dev default: arm64-v8a only — the C++ core (whisper/sherpa/llama)
# is compiled once per ABI, so this is ~3x faster than building all of them and it
# matches every modern test device. Override from the CLI for a store release:
#   ./gradlew assembleRelease -PreactNativeArchitectures=arm64-v8a,armeabi-v7a,x86_64
reactNativeArchitectures=arm64-v8a

# Origin for the ~96 MB of models fetched on first run. They are deliberately NOT in the APK.
#
# Empty = fetch from upstream (GitHub, Hugging Face). Fine for development, not for a release:
# those URLs are outside our control and put first-run success on someone else's uptime.
#
# Set this to a host serving the files under /models/v1/<filename>, using the filenames in
# ModelCatalog. Upstream stays as a fallback, and every download is sha256-verified against the
# catalog either way, so mirroring cannot substitute a different file undetected.
#   ./gradlew assembleRelease -PmodelBaseUrl=https://cdn.innocorelabs.com
modelBaseUrl=

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=true

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Use this property to enable edge-to-edge display support.
# This allows your app to draw behind system bars for an immersive UI.
# Note: Only works with ReactActivity and should not be used with custom Activity.
edgeToEdgeEnabled=false
