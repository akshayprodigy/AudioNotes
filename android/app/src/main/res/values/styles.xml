<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <!-- Every TextView React Native creates goes through the (context) constructor, which
             reads the theme's textViewStyle — so this is the one place a TextView default can be
             set for the whole app. See values-v35 for the item it carries there. -->
        <item name="android:textViewStyle">@style/VerbaleTextView</item>
    </style>

    <style name="VerbaleTextView" parent="Widget.AppCompat.TextView" />

</resources>
