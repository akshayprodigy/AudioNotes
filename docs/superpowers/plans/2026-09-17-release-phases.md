# Release phases — what ships before the production build

*17 September 2026. The founder's 15 Sep decision stands: every partly-done or not-built item of
the improvement report is finished before the production build (except audio-file encryption,
which stays on Android FBE, and Hindi, which stays hidden). This is that work as phases, in
order, each one a spec → plan → code → Pixel run → docs cycle. The store-side items (Play
Console product, listing, website, staged rollout) are the founder's and run in parallel; they
are not phases here.*

| Phase | What | State |
|---|---|---|
| 1 | **Prove 4 and 5 on the Pixel.** Typed record + review queue; ask this meeting + meaning search. | **Done 17 Sep** — two phone-found fixes (`564431f`, `dd214e2`); three spot-checks listed in the ask spec |
| 2 | **Meeting templates** (sub-project 6a). Meeting types — stand-up, 1:1, client, interview, lecture, site walk — as prompt and section variants for the narrator, auto-suggested from the transcript, changeable on the Summary tab, remembered per tag. | **Done 17 Sep** — built by a Sonnet session from its brief; reviewed the same day (report §10): one phone-shape defect fixed (`foldSections`, Pixel 16/16), the suggester's threshold left for the founder to calibrate; the by-hand run (report §7) is the founder's |
| 3 | **Thread memory — decision history + preparation** (sub-project 6b). A thread is the meetings that share a tag; a Pro screen shows what is still open across them, the decisions in order with "changes: …" links found by a rule over the existing item vectors, and the meetings. No generated text. Founder's shape and Pro-only decision of 17 Sep; brief: `docs/superpowers/specs/2026-09-17-phase-3-thread-memory-brief.md`; report: `docs/superpowers/reports/2026-09-17-phase-3-thread-memory.md`. | **Done 17 Sep** — built by a Sonnet session from its brief (report `docs/superpowers/reports/2026-09-17-phase-3-thread-memory.md`); reviewed the same day: gate clear, code faithful; the by-hand run in the report's §7 is the founder's |
| **4** | **Remembered voices** (sub-project 7). A voiceprint per named speaker (`Diarizer::speakerVoices`, measured 17 Sep: the same person across meetings 0.79–0.88, strangers ≤ 0.50), a `people` table, "Sounds like Priya?" with one tap to confirm; off by default, consent copy fixed, forget-all in Settings; Pro only. Brief for a 262k-context builder in two sessions: `docs/superpowers/specs/2026-09-17-phase-4-remembered-voices-brief.md`. | **Done 18 Sep** — C++ done (`41f5158`), brief written 17 Sep; JS screens + gate all clear + report 18 Sep (by-hand Pixel run not run — phone unavailable) |
| 5 | **Custom vocabulary + dictation** (sub-project 8). Vocabulary as correction rules applied after recognition ("in over" → "Innova"; a recogniser prompt was measured and rejected — it drops words on every meeting); rules learned from "Correct the words" or typed in Settings; Pro. Dictation mode: one voice, spoken punctuation, the note as the write-up, the transcript as the verbatim view. Design: `docs/superpowers/specs/2026-09-18-phase-5-vocabulary-and-dictation-design.md`; Session 1 sheet `2026-09-18-phase-5-session-1-execution.md`; Session 2 sheet `2026-09-21-phase-5-session-2-execution.md`; Session 3 (device) sheet `2026-09-21-phase-5-session-3-device-execution.md`. | **Built, reviewed, device-tested 21 Sep** — Sessions 1–3 built by Laguna and reviewed (Session 3 on the 32-bit Galaxy Tab A, the Pixel having died; one review fix `1b20e4b`); only the founder's by-hand run (report §7) remains, on the tablet |
| 5b | **Device fit** — say what this phone cannot run BEFORE the download, and on the Pro screen; nothing device-specific. Three facts in `DeviceFit.kt`: **memory** (the writer needs a 4 GB phone — the 3 GiB gate unchanged, in the GB a phone is sold as; only `llm-qwen` is gated, the meaning index stays), **processor** (the engines are built for armv8.2+fp16+dotprod; Cortex-A53/A73 phones — Helio G25–G85, P60/P70, Snapdragon 425–450/636/660 — are 64-bit without them and would SIGILL on the first transcription; now refused by name at onboarding and at engine load), **disk** (a download is refused with both numbers instead of failing part-way). Founder's rule 21 Sep. Sheet `docs/superpowers/specs/2026-09-21-device-fit-execution.md`. | **Built and reviewed 21 Sep** — Laguna, two runs (`65ec0cc`…`304de11`), faithful; review fixed `device-verify.sh`'s silent no-match pass (`b3d948d`); the tablet's release build shows the onboarding sentence end to end. The founder's by-hand run (report §7, six steps on the tablet) remains, with Phase 5's. **Follow-on decision (founder):** a second, baseline-armv8-a build of the core (`libaudionotes_base.so`, ≈ +7 MB in the arm64 split, chosen at load by the same `cpuFits`) would turn the processor refusal into support for those phones at a slower speed — needs an A53/A73 arm64 phone to verify and measure |
| 6 | **The no-internet build decision, and the release gate** (sub-project 9). **6a:** measure what the INTERNET permission buys (model download, licence server, Crashlytics) against a build without it (Play Asset Delivery, Play Billing alone, Android Vitals); the founder decides. **6b:** the final full gate on a 64-bit phone and the production build. | **6a sheet written 21 Sep** (`docs/superpowers/specs/2026-09-21-phase-6a-internet-decision-execution.md`, a Sonnet session writes the decision brief; no code). 6b blocked on a 64-bit phone (the A07 over adb) |

**Rules that hold across every phase:** one spec and one plan per phase; TDD; every new test
mutation-checked; the Pixel run before "shipped"; the founder pushes; a phase's spec lists any
decision taken in the founder's absence in a §0, to overrule.

**Open device gates carried into Phase 6's final run** (from the launch list): the consent
clip's level fix re-run on a phone; the 90-minute meeting's diarization memory; a third,
non-Pixel non-Samsung phone (MIUI) end to end.
